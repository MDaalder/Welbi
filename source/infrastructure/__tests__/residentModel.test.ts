describe('ResidentModel', () => {
  describe('getAllResidents', () => {
    it('should return all residents from raw data', () => {})

    it('should return a deep copy of all residents from raw data', () => {})
  })
})
